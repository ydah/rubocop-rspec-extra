# frozen_string_literal: true

RSpec.describe Rubocop::Rspec::Extra do
  it "has a version number" do
    expect(Rubocop::Rspec::Extra::VERSION).not_to be_nil
  end

  it "does something useful" do
    expect(false).to be(true)
  end
end
