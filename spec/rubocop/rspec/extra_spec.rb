# frozen_string_literal: true

RSpec.describe Rubocop::Rspec::Extra do
  it "has a version number" do
    expect(Rubocop::Rspec::Extra::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
